# 명지전문대 취업 로드맵 에이전트 — 설계서

> 2026 명지전문대학 RISE 산업 AI 해커톤 | 팀 3인 | 작성일: 2026-08-06

## 한 문장 요약

**원하는 직무를 고르면, 졸업이 보장되는 학기별 로드맵(수강·자격증·현장실습)을 짜주는 전문대 특화 AI 에이전트 — 학교에는 교육과정 진단 리포트를 제공한다.**

---

## 1. 문제 정의

- 전문대는 2~3년 안에 졸업요건·자격증·현장실습·취업을 동시에 끝내야 한다. 4년제와 달리 복구할 시간이 없다: 1학년에 선수과목·자격증 타이밍을 놓치면 만회가 어렵다.
- 그런데 학생이 손에 쥐는 정보는 교육과정표 문서 한 장이다. "네트워크 엔지니어가 되려면 어느 학기에 뭘 듣고 CCNA를 언제 따야 하는가"는 아무도 알려주지 않는다.
- 이 정보격차는 신입생·편입생·유학생에게 더 크다 (→ 공익 앵글: 정보 접근성).
- 학교 포털의 졸업사정 시뮬레이션은 "졸업 가능 여부 **확인**"이다. 우리는 목표 직무에서 역산한 "경로 **설계**"를 한다. 이 구분이 차별화의 핵심이다.

## 2. 심사 기준 매핑

| 심사 항목 | 배점 | 우리의 대응 |
|---|---|---|
| 기술 구현 완성도 | 30 | 검증기+자기수정 루프, 전 학과 파이프라인, 정량 지표 (README에 명시) |
| 문제 정의·기획성 | 20 | "교육과정과 채용시장의 어긋남" 프레임 + 결정 근거 문서화 |
| AI 활용 역량 | 20 | LLM 구조화 추출 파이프라인 + 에이전트 + 임베딩 매칭 (단순 API 호출 아님) |
| 실용성·활용 가능성 | 20 | 트랙 B(학교용 진단 리포트) — 심사위원이 학교 관계자 |
| 산출물 완성도 | 10 | README·아키텍처 다이어그램·지표 표·설계서 (심사가 문서+깃허브 기반) |

## 3. 스코프

### 포함

1. **엔진**: 교육과정표 LLM 추출 파이프라인 + 졸업요건 검증기
2. **트랙 A (학생용)**: 직무 선택 → 학기별 로드맵 생성 웹앱
3. **트랙 B (학교용)**: 교육과정 ↔ 직무역량 정합도 리포트 (화면 1~2개)
4. **데이터**: 전 학과 2계층 수집 (아래 5절)

5. **회원/로그인** (2차 결정, 8/6 저녁): JWT 인증(액세스+리프레시) + **PostgreSQL** — 계정·이수내역·저장 로드맵만 저장. **과목 데이터는 JSON 유지** (동결 스키마 불변). 메인(랜딩) 화면 포함. **착수는 코어 통합(05:00) 이후**

### 제외 (명시적으로 안 하는 것)

- 수강신청 연동, 실제 채용공고 크롤링(시간 남으면 보너스), 모바일 네이티브 앱, 소셜 로그인/이메일 인증
- 전 학과 Tier 1 품질 검증 (20시간 불가 — 지표로 정직하게 공개)

## 4. 아키텍처

```
[수집]  학과 소개 페이지 (faculty/facultyIntro.do?facultyType=코드, 전 학과 동일 구조)
        연도별 교육과정표 게시판 (menu_idx=2207, 첨부파일 HWP/PDF/엑셀)
   ↓
[추출]  LLM 구조화 추출 → JSON 스키마 검증 → 실패 시 재추출 루프
   ↓
[DB]    courses.json (전 학과) + certificates + careers + 졸업요건 룰
   ↓
[매핑]  과목명+인재양성유형+NCS ↔ NCS 직무기술서 요구역량 (임베딩 매칭)
   ↓                                    ↓
[트랙 A] 로드맵 에이전트              [트랙 B] 정합도 리포트
   생성 → 검증기(규칙) → 미달 시 재생성    커버리지 / 결손 / 라벨 불일치
   ↓                                    ↓
[웹]    입력 화면 → 로드맵 뷰 → 리포트 뷰
```

**검증기는 순수 규칙 코드다 (LLM 없음).** LLM이 짠 로드맵을 졸업요건에 대입해 결정론적으로 검사하고, 미달이면 미달 사유를 피드백해 재생성시킨다. 이 self-correction 루프가 기술 점수의 핵심 소재.

## 5. 데이터

### 소스 (확인 완료)

| 소스 | URL | 내용 |
|---|---|---|
| 학과 소개 (전 학과 동일 패턴) | `mjc.ac.kr/faculty/facultyIntro.do?facultyType={코드}&facultyContent=01` | 자격증, 진로, 학제 |
| 연도별 교육과정표 게시판 | `mjc.ac.kr/bbs/data/list.do?menu_idx=2207` | 학과별 교육과정표 첨부 (2026학년도까지) |
| 학과 서브도메인 (일부 학과) | 예: `itc.mjc.ac.kr`(정보통신), `mjcs.mjc.ac.kr`(컴공) | 교과안내 표, 취득 자격증 |
| 졸업요건 | `mjc.ac.kr/ibuilder.do?menu_idx=1942` | 아래 룰 |
| NCS 직무기술서 | ncs.go.kr (공공데이터) | 직무별 요구역량 (트랙 B 비교축) |

### 졸업요건 룰 (검증기 스펙)

| 학제 | 총학점 | 교양 | 전공 | 재학 |
|---|---|---|---|---|
| 2년제 | 75+ | 6+ | 45+ | 4학기+ |
| 3년제 | 110+ | 10+ | 66+ | 6학기+ |

- 교양필수: 인성채플(1), 성경과삶(2) — 입학년도별 상이 가능, Tier 1 학과에서 원본 확인
- 전공심화(연계): 2년제 55학점(전공42+교양9) — v1 범위 밖, 스키마만 대비

### 교육과정표 원본 컬럼 (컴공 페이지에서 확인)

`편성학년 | 편성학기 | 교과목명 | 학점 | 이수구분 | 인재양성유형 | NCS적용 | 현장중심`

**`인재양성유형` = 학교가 이미 만든 과목↔직무 라벨 = 트랙 B의 ground truth.**

### 2계층 품질 전략

| 계층 | 범위 | 기준 |
|---|---|---|
| Tier 1 | 정보통신공학과 (+여유 시 컴퓨터공학과) | 원본 대조 수동 검증. 모든 데모는 여기서 |
| Tier 2 | 나머지 전 학과 | 파이프라인 자동 통과. 학과별 추출 성공률·커버리지 표를 산출물에 포함 |

### courses.json 스키마 (v1 — 0~1h에 팀 확정 후 동결)

```json
{
  "dept_id": "itc",
  "dept_name": "정보통신공학과",
  "faculty_type_code": "A08",
  "years": 3,
  "tier": 1,
  "courses": [
    {
      "course_id": "itc-1-1-prog1",   // 동결 검토 반영: API는 id로만 주고받음 (과목명 문자열 매칭 금지 — Ⅰ/I/1 표기 혼재)
      "name": "프로그래밍언어실습Ⅰ",
      "year": 1,
      "semester": 1,
      "credits": 3,
      "category": "전공",          // 전공 | 교양 | 일반선택
      "required": false,
      "talent_type": "응용소프트웨어 개발자",
      "ncs": true,
      "field_based": false
    }
  ],
  "certificates": ["정보처리산업기사", "CCNA", "..."],
  "careers": ["네트워크 엔지니어", "AI 개발자", "..."],
  "source_url": "...",
  "extraction_confidence": 0.95
}
```

### API 스펙 (v1 — P2↔P3 계약)

```
POST /roadmap
  in:  { dept_id, current_year, current_semester, completed_courses: [course_id], target_job }
  out: { semesters: [ { year, semester, courses: [...], certificates: [...], notes } ],
         validation: { passed, total_credits, major_credits, liberal_credits,
                       details: [ { rule, required, actual, shortfall } ] } }
  # 동결 검토 반영: details는 구조화 배열 — 재생성 프롬프트에 그대로 투입 + 발표 화면 표시
  # 재생성 루프 상한 max_retries=3. 초과 시 passed=false + 부분 로드맵을 "정상 응답"으로 반환 (에러 아님)

GET /report/{dept_id}
  out: { jobs: [ { job, coverage_pct, covered_courses: [...], gaps: [...], label_mismatches: [...] } ] }

GET /depts
  out: [ { dept_id, dept_name, years, tier } ]

# 인증 (2차 결정 추가 — 코어 통합 후 구현)
POST /auth/signup   in: { student_id, name, password, dept_id }
POST /auth/login    in: { student_id, password } → { access_token, refresh_token }
POST /auth/refresh  in: { refresh_token } → { access_token }
GET  /me            → 내 정보 + 이수내역 + 저장된 로드맵
PUT  /me/courses    → 이수내역 저장
```

### DB (PostgreSQL — 유저 데이터 전용)

| 테이블 | 컬럼 |
|---|---|
| users | id, student_id(unique), name, dept_id, password_hash, created_at |
| completed_courses | user_id, course_name, year, semester |
| saved_roadmaps | user_id, target_job, roadmap_json, created_at |

- 실행: `docker compose up -d db` (postgres 컨테이너) — README에 명시
- ORM: SQLAlchemy, 마이그레이션 없이 `create_all` (해커톤 범위)
- **과목/자격증/진로 데이터는 DB에 넣지 않는다** — `data/*.json`이 소스, 동결 스키마 유지

## 6. 화면 (코어 3 + 부가 2)

**코어 (먼저):**
1. **입력**: 학과 선택 → 학년/학기 → 이수 과목 체크 → 목표 직무 선택
2. **로드맵 뷰**: 학기별 타임라인 (과목 + 자격증 타이밍 + 현장실습 배치) + "졸업요건 충족 ✓" 배지
3. **리포트 뷰 (트랙 B)**: 직무별 커버리지 바 + 결손 과목 + 라벨 불일치 목록

**부가 (통합 후):**
4. **메인(랜딩)**: 서비스 소개 + 시작하기
5. **로그인/회원가입**: 학번 기반. 로그인 시 이수내역·저장 로드맵 자동 로드

### 화면 흐름

```
메인(소개) → 로그인/회원가입 → 입력 → 로드맵 뷰
                └ 게스트로 둘러보기 ↗        └ [저장] (로그인 시)
리포트 뷰(트랙 B): 상단 네비 "학교용 리포트" 탭으로 별도 진입
```

- **게스트 모드 필수**: 로그인 미완 컷 시에도 메인→게스트→입력→로드맵 데모 경로 성립
- 로그인의 가치 = 입력 화면 자동 채움 + 로드맵 저장. 데모에서 게스트/로그인 대비로 보여줌
- 트랙 B는 학생 여정과 분리 (발표 때 "학교를 위해서는—" 전환 포인트)

## 7. 3인 분담

| 담당 | 역할 | 산출물 |
|---|---|---|
| **P1** (백엔드·파이썬) | 수집 + LLM 추출 파이프라인 | courses.json 전 학과, 품질 지표 표 |
| **P2** (백엔드·API) | 검증기 + 로드맵 에이전트 + 정합도 계산 | API 서버 |
| **P3** (프론트 + 기획·발표) | 화면 3개, README, 다이어그램, PPT, 회의록 | 문서 전부 + 발표 |

**원칙: 0~1h에 스키마·API 스펙 동결 → 이후 P2·P3는 목데이터로 개발, P1을 기다리지 않는다.**

## 8. 타임라인 (20h)

| 시간 | P1 | P2 | P3 |
|---|---|---|---|
| 0–1h | 셋이 함께: 스키마·API 동결 | ← | ← |
| 1–6h | 정보통신공학과 Tier 1 데이터 완성 | 검증기 + 목데이터 에이전트 | 화면 뼈대 + README 골격 |
| 6–10h | 파이프라인 → 전 학과 투입 | 실데이터 연결 + 재생성 루프 | 로드맵 뷰 완성 |
| 10–13h | 품질 지표 집계 | 트랙 B 정합도 계산 | 리포트 뷰 |
| 13–15h | 통합 + 50개 시나리오 검증 → 지표 산출 | ← | ← |
| 15–18h | 버퍼 | ← | 문서·PPT 완성 |
| 18–20h | 코드 정리·제출 | ← | 발표 리허설 |

### 컷라인 (사전 합의된 축소 규칙)

- **6h**: 파이프라인이 정보통신공학과 하나도 못 뽑으면 → Tier 2를 "서브도메인 구조 동일 학과만"으로 축소
- **15h**: 트랙 B API가 안 붙었으면 → 정적 분석 1회 결과물(HTML 리포트 한 장)로 대체. 발표 훅은 유지
- **로그인/DB (2차 결정 기능)**: 코어 통합(05:00) 이후 착수. **18h(10:00) 시점에 미완이면 그 상태로 컷** — 비로그인 게스트 모드로 데모, 코드는 브랜치에 보존. 코어 데모가 로그인 때문에 깨지는 일은 절대 없게 한다

## 9. 검증 계획 (정량 지표 — README에 그대로 실을 것)

1. **검증기 효과**: 시나리오 50개에서 로드맵 졸업요건 충족률 — 검증기 루프 on vs off (LLM 단독) 비교
2. **파이프라인 품질**: 학과별 추출 성공률, 스키마 검증 통과 파일 수 / 전체
3. **트랙 B**: Tier 1 학과의 직무별 커버리지 수치 + 발견된 라벨 불일치 사례 1~2개 (발표용)

## 10. 리스크

| 리스크 | 대응 |
|---|---|
| 교육과정표 첨부가 HWP 등 이형 포맷 | LLM 구조화 추출로 흡수 (파서 코딩 금지). HWP 파싱 도구 확보됨 |
| 학과별 페이지 구조 불일치 | 게시판 첨부를 1순위 소스로. 서브도메인은 보조 |
| 과목 상세 설명 부재 → 매핑 정밀도 | 과목명+인재양성유형+NCS 코드 묶음 임베딩. NCS 직무기술서로 보강 |
| 학과별 세부 졸업요건(교육운영계획서) 비공개 | 공통 룰만으로 검증기 성립. 여기에 시간 쓰지 않기로 합의 |
| "졸업사정 시뮬 이미 있다" 반박 | "확인 vs 설계" 구분 + 트랙 B 존재. README 첫 문단에 명시 |
| "SMART CARE에 직무역량/경력/학년별 로드맵 메뉴 있다" 반박 | 팀이 실물 확인 완료(2026-08-06, 캡처 보유). 메뉴명은 겹치나 **SMART CARE는 학생이 직접 입력해 등록하는 이력관리 양식**이고, 계획을 생성해주지 않음. 우리는 그 빈칸을 채우는 생성 엔진 + 졸업요건 검증 보장. 방어 문장: "SMART CARE는 로드맵을 등록하는 곳, 우리는 로드맵을 만들어주는 엔진. 우리 출력물이 SMART CARE의 입력물이 된다." **발표에서 심사위원이 묻기 전에 선제 언급할 것** — 비교 슬라이드에 캡처 포함 |

## 11. 발표 훅 (3~4분)

> "LLM은 그럴듯한 거짓말을 합니다. 그래서 저희는 LLM 위에 졸업요건 검증기를 얹었습니다 — 저희가 짜준 로드맵은 100% 졸업 가능합니다. 그리고 같은 엔진으로, 학교 교육과정이 실제 채용시장과 어디서 어긋나는지도 진단했습니다."

## 12. 주요 결정 사항 (1차 회의록 겸용)

| # | 결정 | 근거 |
|---|---|---|
| 1 | 주제 2(교내) 메인 + 접근성 공익 앵글 | 심사위원=학교 관계자, 실데이터 확보 가능. 주제 1 단독은 20h 내 실용성·구현 점수 불리 |
| 2 | C안(로드맵 에이전트) 채택, RAG 챗봇·강의 요약은 기각 | RAG 챗봇은 흔함, 강의 요약은 클로바노트 등 프라이어 아트. C가 전문대 특화 차별화 최대 |
| 3 | 투트랙(학생용+학교용) | 엔진 하나, 산출물 둘. 트랙 B 개발비용 대비 실용성 점수 효율 최대 |
| 4 | 전 학과, 단 2계층 품질 | "파서 N개"가 아니라 "파이프라인 1개에 전 학과 통과". 품질은 지표로 정직 공개 |
| 5 | 심사가 문서+깃허브 중심(발표 3~4분) → 문서 품질에 P3 전담 배치 | 팀 확인 사항 |
| 6 | Tier 1 = 정보통신공학과 (팀원 소속 학과) | 원본 검증 가능, 3년제·자격증 17종·진로 4갈래로 데모 소재 풍부 |
